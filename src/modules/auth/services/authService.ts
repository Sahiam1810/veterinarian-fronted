import type { LoginCredentials } from '../types'

/** Placeholder del servicio de autenticación (API .NET pendiente). */
export async function loginRequest(_credentials: LoginCredentials): Promise<void> {
  await Promise.resolve()
}
